name: CheckSecrets

env:
    test_env: ${{ secrets.test_env }}
    LOCAL_ENV: local_env_value
on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print secrets
      run: |
        echo $test_env
    - name: Print local env
      run: |
        echo $LOCAL_ENV
        